import { describe, test, expect } from 'vitest';
// import { setup, $fetch } from '@nuxt/test-utils';

describe('test example', () => {
  // await setup({
  //   server: true,
  // });

  test('201 status code', () => {
    // const html = await $fetch('/');
    // expect(html).toBe('Test successful.');
    expect(true).toBeTruthy();
  });
});
